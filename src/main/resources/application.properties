# MySQL connection properties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/beba
spring.datasource.username=root
spring.datasource.password=*Dusanmysql11*

# Update new tables (create, create-drop, validate, update)
# Only for testing purpose - comment this in production
spring.jpa.hibernate.ddl-auto=update

# Hibernate SQL dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# TimeZone properties
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jackson.time-zone=Europe/Paris

# Log JPA queries
# Comment this in production
spring.jpa.show-sql=true

############# Logging #################
logging.level.com.dusanweb.beba=TRACE
#logging.file.name=logger.txt

